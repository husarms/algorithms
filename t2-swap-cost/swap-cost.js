/* 
    Given an array of numbers and an array of costs
    Find the minimum cost to re-arrange the letters so the same letters are not adjacent
    T = ['a', 'a', 'b', 'c', 'd', 'a']
    C = [5, 1, 3, 2, 4, 5]
    Minimum cost = 1 (move the 'A' at index 1 for a cost of 1)
*/

function computeNumberOfSwaps(T, C) {

}

const swapCost = swapCost(['a', 'a', 'b', 'c', 'd', 'a'], [5, 1, 3, 2, 4, 5]);
console.log(`Number of swaps: ${numberOfSwaps}`);
